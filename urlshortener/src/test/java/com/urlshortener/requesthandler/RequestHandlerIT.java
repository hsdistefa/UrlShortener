package com.urlshortener.model;

import static org.junit.Assert.assertEquals;

import org.junit.Test;


/**
 * Full stack integration tests
 * Uses DynamoDB local instead of service
 */
public class RequestHandlerIT {
}
